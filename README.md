# Simple OTA Update via UART
Simple program for receiver and transmitter to update receiver via UART communication port. The update can be done with WebServer. Transmitter is connected to network via WiFi or Ethernet.
